[project]
name = "device-systems"
version = "0.1.0"
description = "Api rest"
readme = "README.md"
authors = [
    { name = "Sebastian Hurtado", email = "sehuto96@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "fastapi>=0.141.1",
    "pydantic[email]>=2.13.4",
    "uvicorn[standard]>=0.52.4",
]

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"

[tool.uv.build-backend]
module-name = "app"
module-root = ""
